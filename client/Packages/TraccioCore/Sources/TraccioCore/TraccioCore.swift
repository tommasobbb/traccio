/// Namespace for the TraccioCore package.
///
/// Holds the client's shared entry points (see `jsonDecoder()` in
/// `JSONCoding.swift`). The response models mirror `docs/api/openapi.json` and
/// live under `Models/`.
public enum TraccioCore {}
