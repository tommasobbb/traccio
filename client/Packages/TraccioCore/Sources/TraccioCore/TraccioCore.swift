/// Namespace and version marker for the TraccioCore package.
///
/// This is intentionally minimal scaffolding: it makes the package build and
/// test from the command line before the real content lands. The API models
/// are generated from `docs/api/openapi.json` in a later step (see
/// `tasks/backlog.md`, "TraccioCore Swift package").
public enum TraccioCore {
    /// Version of the client core, bumped as the package gains real content.
    public static let version = "0.0.1"
}
